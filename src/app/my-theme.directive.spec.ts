import { MyThemeDirective } from './my-theme.directive';

describe('MyThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyThemeDirective();
    expect(directive).toBeTruthy();
  });
});
